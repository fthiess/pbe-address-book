# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Vite
*.local
.vite/

# Tests / coverage
coverage/
playwright-report/
test-results/
.playwright/

# Firebase emulator
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
*-debug.log

# Environment / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
*.log
npm-debug.log*

# Offline-restore artifacts (7b-3): the pre-restore safety snapshot, the restore
# report and the Ghost discrepancy report. Every one of them contains the whole
# member directory — REAL PII in a PUBLIC repo. The tool defaults here; keep the
# ignore even if you point --out-dir elsewhere.
restore-artifacts/
