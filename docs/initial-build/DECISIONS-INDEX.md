# PBE Address Book — Decision Index

The subsystem → currently-authoritative-decisions map for [DECISIONS.md](DECISIONS.md). **Consult this file first and read only the entries it cites — never read the whole log into context** (it exceeds 400 KB). Every PR that appends to the log updates the affected line(s) here in the same commit.

How to read a line: chains run oldest → newest; **bold** marks the current word on a matter; plain IDs stand as written but may be amended in detail (each entry's own "*Later updated by*" trailer is authoritative); ~~struck~~ IDs are wholly superseded, kept in the log for their reasoning. `D*` = design decisions, `N*` = implementation notes.

## Architecture & stack

- Foundations: **D2** (custom app, no framework), **D3** (TypeScript end-to-end, Biome), **D4** + **D6** (SPA bulk-download of the full permitted set), **D10** (React + Vite; Node on Cloud Run; Firebase Hosting), **D29** (shadcn/ui; TanStack Table/Virtual).
- Runtime shape: **D83** (single Cloud Run instance, scale-to-zero — the one in-memory cache is authoritative); **both** scaling levels are now pinned to 1 (OFC-209/**N134**) — `--max-instances` is *revision*-scoped only and no gcloud flag clears the service-level umbrella, so `provision-staging.sh` PATCHes the v2 `scaling.maxInstanceCount`. ⚠ Never substitute `--scaling=1` (MANUAL mode — destroys scale-to-zero).
- Overview diagrams: **N100** (`docs/architecture/` — token-palette SVGs, slide + annotated README pairs in light/dark; README `<picture>` embed) → **N128** (extends the convention to a **CI/CD pipeline pair**, `book-cicd-{light,dark}.svg`, embedded in the architecture README — no slide variant, no gold lane, dashed = not built yet, light is the source of truth). ⚠ Both pairs carry a keep-in-step obligation: architecture changes touch all four `book-arch-*`, and any change to `ci.yml` / `deploy-staging.yml` / `verify:gate` / branch protection touches both `book-cicd-*`.

## Read path, caching & performance

- Store & cache: D7 (Firestore system of record; in-memory cache) → D26 (snapshot listener, demoted to safety net by D83) → **D82** (split bulk read: roster projection + self-fetch) → **D83** → **D85** (GCS snapshot for cold-start hydration).
- Dataset compression: D75 → **D84** (brotli-11 precomputed on write, off the event loop).
- HTTP caching: **D73** (content-hashed immutable assets, no-cache HTML) + **N25** (no-cache SPA shell at every route) → **N78** (long-lived-tab version.json toast); ~~D76~~ (conditional GET — retired by **D95** `no-store`, re-verified absent in 7b-1: no bulk `ETag`, `If-None-Match` never `304`s — the exact baseline Phase 7.5 builds on); **N75** (per-role `/api` responses `no-store` on *all* branches) → **D146 (current — read this first)**: Firebase Hosting applies its `firebase.json` `headers` rules to the `/api/**` and `/img/**` **rewrites** and **overrides the backend's `Cache-Control` on 2xx** (4xx pass through), so the delivered posture is set in `firebase.json` — `/api/**` → `no-store`, `/img/**` → `private,…immutable`, ordered **last** because the last matching rule wins per header key (measured, *not* documented by Firebase; guarded by `scripts/lib/hosting-cache-headers.test.ts`). The app keeps its headers as defense-in-depth plus an `onSend` **floor** for header-less branches. App-level conditional read: **N62** + **N63** (the Phase 7.5 design) → **N147** (7.5a: the unified client roster store — one `useRoster` singleton read by Directory *and* Profile, instant render on return trips, background revalidate on Directory mount + tab focus, `error` only when there is nothing to render, and the **epoch fence**: a fetch in flight when `clearRoster()` runs is discarded, so sign-out actually removes the PII from the heap) → **D159** + **N148 (current)** (7.5b as built: the version token is a **per-role content hash** minted in `projectAndCompress` — ⚠ *not* the D85 snapshot version the design named, which was never built — served as the bulk `ETag`; a matching `If-None-Match` earns an empty `304` that keeps `no-store` on every branch, the token lives heap-only beside the data and is zeroed by `clearRoster`, and role-qualification is structural + suffix so a View-As switch can never be `304`d across projections. Delivered behaviour verified live 2026-07-28: Hosting passes the bodyless `304` + headers through the rewrite intact — the D146 concern does not extend to this 3xx).
- Bundle discipline: **D74** → ceiling raised 250 → **270 KB** brotli in 7a-2 (**N124**), attributable to Mixpanel's core build (+30.5 KB; measured 223.8 → 254.5 KB). Each move of that number names the change that caused it. **7b-1 ran the one-time Lighthouse baseline (N134)** — measured 256.8 KB total, but **only 3.4% is deferred and 51% of the entry chunk is unused on the Directory**: `App.tsx` has **no `React.lazy()`/dynamic `import()` at all**, so D74's named split candidates (Admin above all) ship on the critical path (OFC-323) → **D157 (current): that clause was measured across four configurations and deliberately NOT implemented, and OFC-323 is Canceled.** Only **21%** of the 223.3 KB critical path is page-exclusive (Directory 20.4 / Profile 17.3 / Admin 5.8 KB) — the rest is shell, vendor and shared code — so the best available split was worth **6.2 KB (2.8%)**. ⚠ **The Profile page must NOT be lazy-loaded:** newsletter deep links (`/brother/:id`) make it a co-equal entry point, so splitting it puts a serial round trip in front of that traffic. Per-route entry points were priced at ~20 KB and rejected. **Don't re-open this without new evidence — read D157 first.** Byte-budget note: D74's ceiling is a *sum*, so it can neither see nor score this; a critical-path guard was considered and dropped with the split. Slow-load UX: **D119** (threshold-gated loading overlay) — ⚠ **shipped with its reassurance line ungated, so it fired identically warm and cold and blamed the server on every throttled load** (OFC-324, N134) → N140 (the second threshold implemented at all *three* call sites, the Directory escalating to a neutral "Still loading…" instead) → **N141 (current): re-measured live, cold + 2 warm — the line is absent from every run where N134 showed it on all three, and the escalation itself is proved by a positive control (2975 ms against a designed 3000 ms gap).** ⚠ A cold instance is possible at **every** call site — the SPA outlives the scale-to-zero backend — so the *threshold*, not the call site, is what makes the line honest. ⚠ **Measuring this overlay is a minefield of confident false negatives** — polling `page.evaluate` under CPU throttling sees nothing, `textContent` reads the deliberately-hidden line, and the escalation is a *class* change so `attributes: true` is mandatory; N141 point (5) lists all four, OFC-359 tracks committing the harness. **Never accept a null from this harness without a positive control.** Also measured: CLS exceeds threshold on both pages (**OFC-325** — the culprits are the footer and main shell, *not* avatars; D42 already reserves the image box). The Directory's 438 avatar requests were investigated and are **D42's idle-time prefetch working as designed** — they start only after the roster completes, run under `requestIdleCallback`, and are `immutable`-cached, so no contention and no re-fetch (OFC-322, closed not-a-bug). **How to re-measure any of this:** the Lighthouse recipe lives in the repo `README.md` ("Measuring delivery performance") — reach for it for any new page or feature, since the byte budget is a sum and cannot see the critical path.

## Data model & vocabularies

- Collections & ids: **D12** (three collections; private state split), **N6** (single numeric id = Constitution ID).
- Fields: **D13** (classYear `number | null`) → **N150** (current — the string→value parse is shared by Add Brother and Profile edit in `pages/profile/class-year.ts`; blank *and* "unknown" mean `null`, the create sends an explicit `null`, and the helper line reddens rather than rewording, except for the range message), **N34** (middleName retained), **N38** (US-only ZIP validation).
- Names: **D15** (constructed Canonical Name, derived ambiguity; served to the Linter per D58).
- Majors: D14 → **D69** (checked-in vocabulary + seed/reconcile script); interim display names **N29** → **D165 (current — read this first)**: the curated launch vocabulary, **58 codes in 25 colour families**, all `active: true` — historical course *names* folded into their modern entry ("15 — Management / Industrial Management / Engineering Administration") rather than carried as separate retired codes, so no deprecated-course set exists; numbers retired outright and never reused (19, 13) keep their own entry. Family = the leading course number and is what carries a chip colour, so all nine Course 6 codes share one. ⚠ D69's runtime-editable Firestore `majors` collection is **still unbuilt** — the vocabulary remains bundled in `packages/shared/src/majors.ts`. ⚠ `compareCourseCodes` was **broken by this expansion and fixed in the same change**: letter-suffixed codes (`21A`) parsed as bare `21` and alpha sub-codes (`3-C`) returned `NaN`, giving `Array.sort` undefined behaviour — a total-order property test over the whole vocabulary is the guard (OFC-320; natural sort itself is OFC-290/N142).
- Country/state: D18 (ISO country codes) → **D37** (US/CA state-province vocabulary); display names via **N7**.
- Ghost-coupled fields: D56 → D70 → **D81** (uuid capture deferred); **N68** (allowCommentReplyEmail removed entirely).

## Auth & sessions

- Ghost bridge: D20 → **D104** (alg-pin, nonce, redirect allowlist) → **D105** (Ghost accepted as single point of compromise); implementation **N1** (RS512, Node crypto), **N2** (one live relay, hardcoded callback allowlist) → **D161** (staging's allowlist entry repointed to `book-staging.pbe400.org`; the value moved, D104's reasoning did not). That is the **Book → Ghost** direction; the reverse is **D139**. ⚠ The allowlist lives in **two byte-identical copies** and the theme is deployed to Ghost **by hand** — no CI can catch drift.
- Ghost → Book (account entry points): **D139** (Phase 7.6 — the newsletter theme's account links and its `/account/` + `/membership/` routes all hand off to Book's `/brother/me/edit`, behind a `book_url` theme setting defaulting to production so cutover needs no action; the redirect is unconditional because the D20 bridge already handles no-session arrivals). ⚠ Repointed links must **drop `data-portal`** — that attribute makes Ghost's Portal script intercept the click and silently defeat the redirect regardless of the `href`. → **D161** (Ghost-staging's `book_url` now `https://book-staging.pbe400.org`). ⚠ `book_url` is a **Ghost admin setting, not code** — a bridge repoint without it leaves newsletter links on the old origin.
- Sessions: D22 (4-hour cap) → **D109** (non-destructive 401/403 recovery) → **D125** (sessions + nonce persisted in Firestore) → **N53** (active revocation on trust withdrawal) → **N76** (central 401 interceptor → signed-out; carve-out: the edit-form Save path keeps the form per D109) → **N98** (full D109 recovery built: a child-window re-auth resumes the held request with its original `If-Match`, deduped + was-authenticated-gated; the directory read recovers in place, OFC-153; **amends D109** — image retry is a re-arm-after-re-auth not status-aware per-image re-auth, and the `sessionStorage` draft stash is dropped for in-memory + a recover-here message); cookie must be named `__session` **N5**; JWKS persisted across cold starts **D87**.
- Gate liveness check: **OFC-147** (present-and-de-brothered session → 401 + destroy) → **D130** (also a role-**downgrade** re-check: live cache role below the session snapshot → 401 + destroy, OFC-239; the free per-request backstop to N53's active revocation, now that role is cache-resident per D128).
- Identity: **D21** (IdentityProvider seam); **D97** (email uniqueness via in-memory index; alias clause dropped by N65); **N8** (de-brothered sign-in denied).

## Permissions & visibility projection

- Read projection: D5 → D16 → **D19** (three roles, three tiers) → **D82**; exhaustive keyof-Profile tables **N9**; staff-internal classes **N10**; whole-record omission **D115** (de-brothered) + **D124** (unlisted); `role` on the profile, **public** — **D128** (reverses OFC-139's staff-only; stored optional, normalized→brother at hydration).
- Write side: **D106** (field allowlist, object predicate, last-admin invariant) + **N70** (managers can't write privacy-hidden toggle fields); role write **D128** (re-pathed to `PUT /api/profiles/{id}/role`, protected field, last-admin over `ProfileCache.adminCount`; supersedes N44/N50's `users`-doc model) → **D129** (last-admin counts only *usable* admins — `isUsableAdmin`: living, non-de-brothered, has usable email; nominal-only admins were an org-lockout hole, OFC-241) → **D130** (enforce the usable-admin invariant across *all five* removal/transition paths — role/delete/mark-deceased/de-brother/PATCH-email — via `cache.isSoleUsableAdmin`; a promote-guard rejects making an ineligible brother admin; and the session gate 401s a role **downgrade** re-check, OFC-239) → **N86** (live-test fixes: promote-guard covers *any* staff role, not just admin; the last-admin/promote refusals are surfaced in the mark-deceased/de-brother/role UIs — they were silently swallowed) → **D135** (clearing a previously-usable email is gated behind a client-side "this locks *name* out" confirmation on all three edit paths; no new server guard — the general clear stays soft, the sole-usable-admin case keeps its D130 hard block; OFC-272) + **N105** (`wouldClearUsableEmail` pure predicate; and the PATCH-email `409 last_admin` now mapped client-side — the one path N86 left throwing the generic banner).
- Impersonation: **N31** (admins step *down* a role; server-side effective role).

## Privacy & consent

- Consent toggles & copy: D45 → **D89** (MITAA opt-in) → **D93** (third-party data defaults off) → **D113** (consequence-copy model) → **N68** → **N102** (switch copy — label/whenOn/whenOff — folded off `consent.ts` into the help-content registry, Option A; `ConsentSwitch` takes an `entryKey`) → **N103** (the counterfactual is dropped from the `?` as redundant with the inline consequence — amends D113; a switch's `?` shows only its optional static `toggleTip`, so only MITAA and Listed keep one; the `?` unified onto `ControlHelp`) → **N107** (6b-5: the `shareEmergency`/`shareSpousePartner` toggles relocated into the Privacy & consent group beside the reachability switches, and their off-copy rewritten to name the field so each row self-identifies away from it; **OFC-268 declined — D93 stands**, emergency/spouse stay opt-in — OFC-270) → **N115** (one canonical switch order across the Profile view and edit pages — Email, Telephone, Mailing, Emergency, Spouse, PBE News, MITAA, Directory listing; the view digest completed with the Emergency/Spouse lines and an always-shown Directory-listing line — OFC-278) → **N117** (swaps the final pair back — …PBE News, Directory listing, MITAA — so the beyond-brotherhood switch sits last rather than between two brotherhood-internal ones — OFC-282) → **D163** (⚠ **reverses D93's default**, not its classification: `shareEmergency`/`shareSpousePartner` now default **shared** — the only reason to enter an emergency contact or a spouse is to share it, and emergency data nobody may read has no function. Third pass over this question; D93 and the OFC-268 decline at N107 both went the other way, so bring a new argument before a fourth. ⚠ `DEFAULT_PRIVACY` covers the **create route only** — the Phase 8 bulk-loader must write the same defaults or every real brother lands opt-out; the all-`false` blocks in `cache.ts`/`patch.ts` are fail-closed fallbacks and stay — OFC-373 — current).
- Mentoring opt-in: **D166 → N160 (current)**. **N160** moves the switch out of Privacy & consent into **Professional & personal, beneath Courses**, so the edit control sits where the view read-out does (Forrest's call). ⚠ **It does NOT reverse OFC-270/N107** — that rule governs *share* toggles bound to an adjacent field's visibility; this switch *is* the data, so it goes with its section. The e2e assertion encoding N107 was **narrowed** (no `Share …` switch, plus an exact count), not deleted. ⚠ **The lock is now per-control**: the switch no longer inherits a locked `Section`, so dropping `locked={consentLocked}` would silently let a manager write it with nothing else looking wrong — the manager-lock e2e test is what guards it. ⚠ Help key moved to `profile.willingToMentor` because the generated manual groups **by key prefix**. D166's second argument for `consent`-over-`editable` (keeping the privacy block uniformly locked) is now obsolete; the first (a manager must not volunteer another's time) is why the rule stands. **D166** — `willingToMentor`, default off, shipped ahead of the mentoring programme (OFC-255) because launch is the peak of profile traffic and an opt-in gathered later has to be chased. ⚠ **The only field that is `public` on read and `consent` on write**, and both halves are load-bearing: public so brothers can be *found* (classifying it `restricted` beside the `allow*` switches it sits next to in the UI would silently make the feature useless), consent so a manager cannot volunteer another brother's time (the D124 rule; **amends the ticket**, which said managers could). ⚠ **Never read the raw field on a display or filter surface — call `isWillingToMentor`** (`packages/shared/src/mentoring.ts`), which also excludes deceased brothers; the stored value is deliberately *not* force-cleared at mark-deceased (unlike `allowNewsletterEmail`, which drives a real outbound action), and the CSV carries the stored value because an export is a dump of state. ⚠ No backfill exists or should be added — absence normalizes to `false` at the hydration boundary, so absence and declining are the same state by design (OFC-386).
- Posture: **D77** (no CCPA machinery) + **D116** (persistent footer privacy notice).
- PII egress: **D95** (`no-store` PII endpoints; sign-out control) → **D146** (D95 was *unenforced on the wire* until 7b-1 — Hosting overrode `no-store` with `no-cache, must-revalidate` on every 2xx PII response; enforcement now lives in `firebase.json`, OFC-212), **D88** (Mixpanel identity drops name), **D80** (mark-deceased consent snapshot; narrowed by N68; Ghost member now deleted/re-created rather than left subscribed-off — **D134**), **D138** (Mixpanel session replay refused — it would carry other brothers' PII around the D5/D82 projection; the core build makes the refusal structural).

## Verification

- **D28** (verification coupled to content edits) → **D48** (surfacing + staleness nudge) → **D68** (CSV import ignores verification fields) → **D80** (snapshot on mark-deceased) → **N73** (read reclassified public — verification visible to all brothers, verifier Canonical Name shown; write coupling unchanged) → **N121** (who may confirm, recorded at last: the verify endpoint is **owner-or-staff** — staff see "Mark as verified" — while an *edit* by anyone other than the owner clears the mark; the two are independent, and "only the brother himself can confirm" is a **false** rationale that had been written and corrected twice — current); endpoint **N40**.

## Directory page

- Columns & grid: **D33** → **N16** (resizable, keyboard-operable) → **N27** (double-click auto-fit) → **N112** (auto-fit measures the full course-chip strip, not the primary only — OFC-277) → **N143** (auto-fit measures the *rendered* cell: the Name column's `font-medium` weight and each highlight mark's padding — OFC-358); **N17** (labels/status treatments), **N22** (4-digit Class year), **N15** (column lens mirrored to URL); Course column shows ALL courses as chips **D136**/**N106** (amends D33's primary-only; sort still keyed on primary), header background fills past the last column **N106** (OFC-262); course chip + aligned name unified as `CourseChipName` (shared by the Course filter and the Profile course picker) **N108** (refines N106) → **N111** (filter checkbox aligns to a wrapping name's first line); sort **D34** → **N142** (a per-column `compare` hook; Course orders by course number via the shared `compareCourseCodes`, not as text — OFC-290); virtualization & thumbnail prefetch **D42** (images app-served per D126); horizontal scrollbar: grid fills to the viewport bottom so the bottom-edge scrollbar is on-screen **N85** (measured max-height; corrects **N84**'s overlay-scrollbar root cause), with **N84**'s `.always-scrollbars` (classic non-overlay) kept as a complementary always-visible-scrollbar improvement — OFC-205.
- Behavior: **D36** (deceased default-hidden, dual marker), **D38** (filter panel; filterable ⟺ visible) + **N51** (always returns collapsed) + **N81** (one-sided open-bound year/ID ranges) + **N110** (multi-selects dismiss on outside-click/Escape via `useDetailsAutoClose`), **D39** (stars) + **N80** (masthead clean-slate clears Starred-only) + **N99** (Star also on the Profile page; the set hoisted to a shell provider shared by the Directory and Profile — OFC-256), **D40** (row opens the Profile page), **N26** (short search placeholder); **N92** (mobile "Options" fold — below `md`, search stays visible and the toggles/columns/filters/action-bar collapse into one disclosure, closed by default) → **N149 (current — the phone Directory)**: the fold also carries a phone-only **Sort control** (two native selects driving new `setSortKey`/`setDirection` actions, *not* `toggleSort` — a dropdown moves only the dimension it names), because a column header was the app's only sort affordance and there are no headers below `md`; the offered fields are **Canonical Name + the lens's visible sortable fields** (Forrest's call — mirrors the desktop "sort only what you can see" rule), ⚠ *plus the active key when the lens hides it*, or a `?sort=` deep link leaves the select misreporting the order on screen; and the picker is worded **"Fields"** below `md` (cards, not columns) — visible vocabulary only, with `cols`, the localStorage key, and the analytics names unchanged.
- Employer (search by company): **D164** — a substring filter + an optional off-by-default column, both available to **every** role, because `employerName` is a **public** field already in every roster projection, so the addition costs no bytes and needs no gate (⚠ check a field's visibility class before designing any new filter — a `toggle`/`restricted` field is a much larger conversation). **Amends PRD §5.6.4**'s MVP exclusion; the one-*current*-employer limitation it cited is unchanged and now stated in the field's `?`. ⚠ **Job Title stays out** and adding it "for symmetry" is a mistake — one job has dozens of titles, so the filter would mislead; multiple employers, history and an industry vocabulary belong to mentor search (OFC-255).
- Willing to mentor (column + filter): **D166** — an optional off-by-default **"Willing to Mentor"** column and an **Any / Yes** filter, both all-roles (the field is public, so D164's "check the visibility class first" rule lands the same way). ⚠ There is deliberately **no "No"** option and the cell is **Yes / em-dash**, not Yes/No: a stored `false` conflates "declined" with "never opened his profile", which is most of the roster on day one. ⚠ `sortValue` is `null` (not `0`) for not-willing, so opted-in brothers head the list in *both* directions — the `role` column's shape, and the reason the sort direction is near enough a no-op. Full reasoning under Privacy above.
- Header layout: **N154 (current)** — the heading/control-row break is pinned to a breakpoint (`sm:flex-col` … `lg:flex-row`), never left to `flex-wrap`, because wrapping on content made the *count line's text width* a layout input: a filter change relayouted the header (OFC-375). Scoped to 640–1024 on purpose — the phone keeps its side-by-side header and its vertical space (OFC-211), residual width-jitter knowingly accepted there.
- Manager/admin action bar & selection: D41 (bulk-delete since dropped by D100; exports per D90/D92) → **N79** (selection persists across search/filter/sort/navigation — D41's clear-on-view-change reversed; a per-instance in-memory bucket beyond D31's three; select-all unions the view; Export spans the whole selection; explicit Clear); **N99** (a subtle vertical rule separates the Select column from the adjacent universal Star to reduce staff misclicks — OFC-64); **D167 (current)** — a second consumer of the selection, **Copy Emails**, putting the selected brothers' addresses on the clipboard as an RFC 5322 recipient list (OFC-391). ⚠ **It honours `privacy.shareEmail` at EVERY role, admins included — deliberately departing from D19 and from the CSV export**, because a `To:` line publishes an address to the other recipients while a CSV goes only to the exporter; do not "harmonize" it with the projection. ⚠ **The skip classification tests the privacy *flag* before the email *value***, which is what makes a manager and an admin tally the same selection identically (only the admin receives an address behind an off toggle) — a unit test pins it. Also excludes deceased/de-brothered (D80's outbound-action reasoning), has **no whole-view fallback** unlike Export, and quotes display names always (an unquoted `.` is not `atext` and `(` opens an RFC 5322 comment). ⚠ **The address half is SCREENED, not escaped** (`isEmittableAddress`) — it sits between `<` and `>` in a comma-separated list so it cannot be escaped, and Book's `EMAIL_RE` accepts `x@evil.com>,y.z`, which would inject an unselected recipient; a review finding, with a test asserting the premise so a future `EMAIL_RE` tightening surfaces rather than leaving a dead guard. `formatRecipient` is deliberately not exported from the package index. Rules live in `packages/shared/src/email-recipients.ts`; read its module note before changing any of them. **N163 (current)** — the result notice anchors `absolute top-full` to the **action bar**, never `fixed` to the viewport: ⚠ two viewport-fixed attempts each failed a different end of the page (a top percentage covered the action bar and the phone's search box; the bottom edge stranded it a screenful from the button), and the bar-anchored version is correct by construction at every viewport height and every amount of content above it — **a percentage offset was the wrong tool, not the wrong number**. Same entry: a one-line notice is now shorter than a two-line one (`items-center` **plus** `-my-2` on the dismiss button so its 44px hit area overhangs the padding box instead of setting the height — ⚠ `items-center` alone does not fix it). Guarded by orderings, never coordinates (N154). **N162** settles the one question D167 left open: **`allowNewsletterEmail` is NOT consulted, permanently** — it is consent for *the newsletter*, not a general permission-to-email (Forrest's call), so ⚠ nothing in Book should start treating it as a do-not-contact flag.

## Name search

- D35 (fuzzy + phonetic over tokenized names) → D66 (A/B criterion) → **N19** (Beider-Morse via bmpm) → **D110** (Web Worker; memo dropped) → **D123** (curated nickname expansion) → **N83** (fold atomic Latin letters NFKD can't — ø/æ/ß/…; worker result ⊇ substring match, so a query never drops a hit it once showed); package + highlighting **N20**.

## Profile page

- Model: **D43** (view/edit, two-up layout) + **N33** (edit mode accumulates no history), **D44** (one layout, four role projections), **D50** (validation/save/conflict) with guard order **N11** — ⚠ D50 shares the *validator*, not the string→value parse in front of it, which is what drifted in **N150**.
- Controls: **D46** → **N36** (Radix Combobox, editable address, sanitized repeatables) → **N108** (Combobox gains a `renderOption` prop; the course picker shows chips via the shared `CourseChipName` — OFC-265 follow-up); polish N35 → **N37** (incl. phone canonicalization) → **N107** (6b-5: the emergency/spouse share toggles unified into the Privacy & consent group; Admin Note promoted to its own "Administrative" view section, left column under Preferences; repeatable Remove-button centring + spacing — OFC-270/271/260) → **N109** (live-test: the repeatable spacing was inert — `space-y` cancelled by the rows' `m-0` → 0px; real fix is flex `gap` + `self-start` on the Add button; and the "Administrative" Admin-Note section reaches the edit page too — current).
- Headshot: **D47** (crop-on-upload, staged until Save) → **D158** (click/Enter opens the full **512² square** in a modal — the stored image the page already downloaded, so it costs no bytes; opt-in per call site, never over the initials avatar — OFC-353).
- Layout chrome: **N144** (a full-width section renders its own `Band` — rule + rhythm — so a section that renders nothing leaves no stranded separator behind it; OFC-318).
- Deceased: D49 → **D122** (birth/death years, b./d. display) → ~~N154~~ → N155 → N156 → **N157 (current)** (OFC-376, four rounds — read this one first. **The iOS native date control adds its padding *outside* a declared width**, so any width you give it lands 24px wide there. Fix: the control carries **no box of its own** (`border-0 bg-transparent p-0`) and a **shell div** draws the border/inset/focus ring — with no padding on the control there is nothing to add outside it, so the shell's `w-full` lands on the grid column and the field aligns with `Death year`. It also keeps its **own row** (N155's insurance). ⚠ Focus ring is `:focus-within` on the shell — fires on pointer focus, unlike its siblings' `:focus-visible`; `has-[:focus-visible]` was rejected because `:has()` would drop the ring on an older iPad. ⚠ **Unreproducible off real Apple hardware** — Chromium, desktop WebKit *and* iPad device emulation all size it correctly (measured, N156's table; don't retry), and Chrome-on-iPad is not a second engine. ⚠ Chromium-only e2e guards the *layout*, never the behaviour.)
- Prev/next: N32 → N45 → **N52** (sessionStorage stash of the displayed set; pure derivation).
- "← Directory" affordance: OFC-145 (a real `<Link to="/">` when there is nothing to pop, a `<button>` when there is — an `href` cannot POP the chain) → OFC-198 (matched to Prev/Next in size/weight/colour, deliberately still borderless) → **N149 (current)**: UAT reversed the borderless half — it is now `StepButton`'s chrome property for property, and all three copies (Profile, Admin, Add Brother — already drifted apart) collapse into one `components/BackToDirectory.tsx`. ⚠ The OFC-145 semantics live *only* there now; the e2e that assert `link` vs `button` by role are what guard them.

## Images

- Storage: **D8** + **D17** (versioned, immutable, WEBP in GCS) → **D94** (3-month purge of superseded versions).
- Serving: ~~D23~~ (CDN signed cookies) → **D126** (app-served from the private bucket via `/img/*`; no CDN, no load balancer) → **D146** (D126's `private` was being *delivered* as `public` — the SPA asset rule's `.webp` glob also matches a headshot through the `/img/**` rewrite; enforcement moved to `firebase.json`, OFC-321); thumbnails D9 → D42 → **D126**; strict path parse + effective-role visibility **N43**. ⚠ The Directory currently requests every thumbnail on first load, not the visible ones (**OFC-322**, N134).
- Pipeline: **N42** → **N47** (seams, ordering, purge) + **N48** (live-test fixes); regenerate-thumbnails feature dropped **D114**.

## Ghost sync

- Frame: **D54** (one composite system), **D55** (single-master, Book authoritative; the read-only-into-Book invariant restored by N69).
- Push path: D96 → **N65** (Ghost-first-gated update; prior-email alias dropped); pushed field set per **N66** + **N68**; write ordering/compensation **D98**; lifecycle seam N41 → **N67** (5b split, roster stub).
- Email↔Ghost-record invariant (a brother has a Ghost member iff living + not-de-brothered + usable email — the `shouldHaveGhostMember` predicate): **D133** (auto-create on the PATCH that adds the email) → **N96** (implemented: PATCH create/delete/update lifecycle; email removed from the create surface — `POST` 422s it, a create is Book-only; a Ghost-member collision is rejected on `email` not linked — Option B; de-brother-reverse gate unified on the predicate) → **N114** (Option-B collision guard extended to the email-change PUT — an email *change* that collides `422`s on `email`, not a generic `502`; Ghost's PUT-collision `422` verified against ghost-staging — OFC-276) + **D134** (mark-deceased now deletes the member and un-mark re-creates it — amends D80; audit exempts a deceased brother like a de-brothered one) → **N133** (the two **reverse re-create** paths — de-brother reinstate + deceased-clear — now surface the duplicate-email collision as `422` on `email` instead of masking it as a generic `ghost_create_failed` `502`; Option B, closing the last create sites that swallowed the typed error — OFC-316; abort-clean N65 preserved; ⚠ diagnostic gap found — the Ghost HTTP status is logged nowhere on this path, ticketed).
- Newsletter flag & audit: ~~D103~~ (bidirectional write-back — reverted) → **N69** (alignment audit fully read-only; bounce report a separate CSV; one generic outage screen); event-fetch bound **N97** (audit/bounce member-events fetched with a 24-month NQL date bound, not the whole append-only history — OFC-231; a bigger page `limit` is foreclosed by Ghost 6.0, OFC-217) → **N113** (fixes N97's field name — the bound must filter `data.created_at`, not the bare `created_at`, which Ghost's `/members/events` allowlist rejects `400`; and makes the audit's *advisory* newsletter-events read swallow failure to `[]` while the bounce report's read still surfaces 502 — OFC-275).
- Ghost-less brothers always tolerated: **N72**.

## Linter roster

- D27 → **D58** (read-only roster endpoint, canonical names served) → **D78** (in-code Google-JWKS verification, subject-pinned) → **D112** (versioned contract + stale-SPA refresh prompt).

## MITAA & Mailman

- MITAA: D59 (manual, low-trust) → **D89** (opt-in default) → **D90** (dedicated consent-aware export; de-brothered excluded per D115).
- Mailman: **D60** (out of MVP; replacement directions recorded).

## API conventions

- Surface: **D24** (as amended by D82/D95/D112/D126).
- Concurrency: **D25** (optimistic, on updateTime) → N13 → **N46** (quoted ETag; If-Match normalized); structural write checks **N12**.
- Endpoints: **N28** (`POST /api/exports` — widened by **D167** with a third `clipboard` scope for the Copy Emails egress, sharing one audit trail rather than opening a second door; the OFC-117 count ceiling and staff gate unchanged), **N40** (`PUT …/deceased`), **N44**; create flow N71 → N72 → **N96** (create is Book-only — email removed from `POST /api/profiles`, which 422s one; the Ghost member is minted later on the email-adding PATCH); single-record read `no-store` on all branches **N75**.

## Security hardening

- **D86** (rate limits), D107 → **N54** (security headers; CSP hashes CI-pinned), **D108** (DevIdentityProvider lockout, four layers), **D64** (Google-managed TLS), **N55** (generic 500 body; ungated sign-out), **D105** (threat posture) + **N56** (accepted residual risks).
- CodeQL baseline: **N88** (triaged all 8 `main` alerts → 0 open: fixed the `EMAIL_RE` polynomial-ReDoS via a 254-char cap, added `permissions: contents:read` to the CI gate, and escaped backslashes-before-pipes in the Ghost-audit Markdown escaper `cell()`; dismissed-with-reason the untrusted-checkout, test-RSA-key, and three `writeRateLimit`-config rate-limit false-positives; OFC-152). Making CodeQL a *required* check needs branch protection created from scratch (main was unprotected) → **N91** (branch protection created 2026-07-14: require PR + 0 approvals, CodeQL + Verify gate as `strict` required checks, force-push/delete off, admin bypass allowed; OFC-246) → admin bypass has since been **closed** (`enforce_admins: true`), recorded belatedly in **D143**, whose safety argument depends on it — ⚠ flipping it back to `false` silently breaks D143. The untrusted-checkout alert re-raises when its checkout line changes (#1→#19).
- Dependency/supply-chain hygiene: **N74** (transitive-dep advisory policy — assess reachability, prefer `overrides` when no patched parent version exists; the uuid CVE-2026-41907 fix, Dependabot #7) → **N89** (accept `@opentelemetry/core` as a documented dev-only residual — dev-only via firebase-tools, no clean upstream fix, an `overrides` pin would break pubsub's `^1.30.1`; revisit when upstream moves to otel ≥ 2.8; OFC-234) → **N159 (current)** (8 alerts triaged, **none reachable as deployed** — the reachability arguments are recorded per package and will apply again; four transitive patches taken as **one PR with one commit each** — the PR #130 bundling pattern (one deploy instead of four `strict`-branch-protection rebase cycles) refined so the per-dependency lockfile diff stays reviewable and revertable; `fast-uri` and `postcss` deliberately overshoot the alerts' minimum versions; `re2` + `@hono/node-server` **dismissed** as dev-only per N89; react-router's v8 major deferred past the blackout. ⚠ **`npm update` on Windows silently prunes `@emnapi/runtime` from the lockfile every time** — a `sharp`/Tailwind wasm optional dep that a Linux `npm ci` needs: diff the lockfile's `packages` **key set**, not its lines, on any lockfile work from this workstation; OFC-380) → **N161 (current)** (react-router taken to **7.18.2**, the **backport** of the same RSC-CSRF fix 8.3.0 shipped — so the v8 major, OFC-383, is no longer a security question at all, only a stale-alert question. ⚠ **An advisory's `first_patched_version` is a claim about the past:** GitHub's was written 4 days *before* the 7.x backport existed and was never revised, which is exactly how N159 concluded "major-only." Check the registry's version list and read changelogs newer than the advisory's own timestamp. ⚠ **`npm audit` still flags 7.18.2 as HIGH** — the stale range `>= 7.12.0, < 8.3.0` catches the fixed version, and `fixAvailable` proposes a *downgrade* to 7.11.0; read the range, not the verdict. ⚠ The `@emnapi/runtime` prune is now known to be **pre-existing lockfile drift** that a bare `npm install --package-lock-only` reproduces on untouched `main` — so it will ride into *any* future dependency PR; use **`npm ci`, not `npm install`**, to sync `node_modules` when a lockfile is hand-corrected; alert #18, OFC-383/OFC-385).

## Logging & analytics

- Logging/audit: D61 → **D91** (no audit-log egress to external LLMs) → **D92** (export audit via notify endpoint); stream structure **N14**. Inherited gaps surveyed in **N122** — 7a-3 split into three sub-sessions to close them: **7a-3a** (audit stream, **N126**, done), **7a-3b** (diagnostic stream + access-log decision, **D142**/**N127**, done), **7a-3c** (observability infra, **D144**/**N129**/**N130**, done + **live-verified** on staging 2026-07-24). **Observability infra (7a-3c, D144/N129):** `infra/provision-observability.sh` (Forrest-run, idempotent) provisions the audit **retention bucket** (90-day, P16) + **sink** on `jsonPayload.logType="audit"` (making `audit-log.ts`'s "routes to its longer-retention bucket" true), two **log-based metrics** (`book_auth_signin_denied`, `book_auth_jwks_failure` — distinct per N126), a **sign-in-denial burst alert** via a Cloud Monitoring email channel (Google sends it; Book has no mail wiring), and the **keyless `book-log-reader` SA** scoped to the audit stream by an IAM-conditioned `roles/logging.viewAccessor` on the bucket view (**not** project-wide `logging.viewer`). ⚠ **D91 stops at the SA** — provisioned, never wired to a cloud LLM; the crux any future OFC-214 alert→Slack/Claude flow must reconcile (Claude is an external LLM; the audit stream is still the who-did-what graph). ⚠ **No app/TS code changed** and infra is outside `verify:gate` — real verification is the live run + synthetic-denial test (fire >10 bogus `POST /api/auth/session`, watch the alert email), not CI. ⚠ **Deferred watchdogs ticketed** (cache-age/listener-disconnect, rate-limit/abuse, newsletter-reconcile-volume, JWKS-failure alert) — each needs a signal not emitted yet; the D99 consolidated health-check job is still unbuilt. **N127** (7a-3b, as built): the diagnostic stream is now the injectable `DiagnosticLog` module (`audit/diagnostic-log.ts`) — sibling to `AuditLog`, one `logType: "diagnostic"` across every severity (severity is the discriminator, not a second logType; the old `logType: "error"` is **retired**), a **severity-routed** default sink (INFO/DEBUG→stdout, WARN/ERROR→stderr), an injectable sink + injected clock, a default `diagnosticLog` singleton for the deep modules. The ~nine ad-hoc `console.*`/`stderr` sites are migrated onto it. **P10 on the diagnostic stream** is enforced by shape (constant `message`, IDs/names on structured slots) **and** a scrubber (`audit/scrub.ts`) that redacts email + conservative phone from the free-text `message`/`detail`/`stack` — the Ghost-error email being the real vector — **tested**, and **linear/no-ReDoS (N88)**. ⚠ **Access stream = the platform request log (Cloud Run + Firebase Hosting); Book builds no app-side request logger** — now the formal decision **D142** (Fork A settled 2026-07-21), Fastify stays `logger: false`, documented in §6.1 so it is not re-flagged as a gap. ⚠ The scrubber's residual is an embedded *name* (not machine-recognizable); ⚠ **`EMAIL_RE` must require a dotted TLD + exclude `/`/`:`** or it eats scoped-npm-package stack frames (`@grpc/grpc-js`…) and guts an ERROR stack (the OFC-149 fidelity guarantee — review caught the naive draft); ⚠ the 500 handler now emits `logType: "diagnostic"` ERROR. **N126** (7a-3a, as built): sign-in now audits `auth.signin` (`ok` with actor / `denied` with a coarse `reason` code, no actor); a transient JWKS-endpoint fault audits the **distinct** `auth.jwks` (kept out of the sign-in-denial metric — `ghost-provider` stops collapsing `JwtKeyResolutionError` into `invalid_token` and tags the `AuthError` `category: "jwks"`, client-facing `401 invalid_token` unchanged) — while a **no-matching-`kid`** (a forged/rotated-away key id) is classified a **denial**, not `jwks` (`jwt-verify.isNoMatchingKeyError`), so a forged-token burst can't hide from the denial alert; and `ghost.push` (declared, zero call sites) now emits from an **`AuditingGhostLifecycle` decorator** wrapping the seam, constructed per-request, so every create/update/delete — **including a failed push** the Ghost-first abort (N65) would otherwise leave unaudited — is recorded once. `AuditEntry` gained optional `actorId` (pre-auth events), `reason`, and `op` — all labels, names-not-values intact. ⚠ **Access stream = Cloud Run's platform request log; Book builds no app-side request logger** (Fork A, §6.1). ⚠ Auth audits are **unguarded** like every other `audit.record` (sink-write hardening deferred cross-cutting, **OFC-301**).
- Analytics identity: D62 → D88 → D137 → **N123** (as built, 7a-1): the `distinct_id` is the Ghost member **uuid**, fetched from the Admin API at sign-in and held on `Identity` (*not* persisted — D81 stays unreversed, and D134's delete/re-create is why a stored uuid would go stale), returned by `GET /api/me` as an **optional** top-level field. Implementation: a narrow `GhostMemberLookup` seam on the existing `GhostAdminReader`, injected optionally; the lookup runs **last** in `createSession`, after the sign-in is already granted, so a denied caller makes no Ghost call. ⚠ The project runs **Simplified ID Merge**, under which two `$user_id`s can **never** be merged — so both halves of the composite system must emit an identical `$user_id`, D62's "Identity Merge absorbs an email change" premise is false for this configuration, and **`identify()` must be conditional on the uuid being present** rather than falling back to another key. ⚠ Sign-in's **only** Ghost HTTP call, and the only step allowed to fail; `GhostAdminHttp.request` gained an opt-in `timeoutMs` used **here alone** — the report/write surfaces stay unbounded (**OFC-294**). ⚠ **A session can reach Book with no uuid and is then permanently anonymous — and this is observed, not hypothetical (OFC-369, open):** the guard is right (a fallback key would be unrepairable under Simplified ID Merge), but `trackPageView` fires for *any* authenticated session (`useAnalytics.ts`), so such a session emits events under an anonymous device id with **no profile**, hence no `Constitution ID` and no `Role`. **One** instance, not two — the tester split across an identified and an unidentified cluster under `app = book`; the second alleged instance (a profile with a Ghost-side `$email` but no Constitution ID) was **retired by N153** as the expected shape for a newsletter-only visitor. **Diagnosed 2026-07-29, unfixed:** `lookupMemberUuid` is fail-soft by design (D137 — every failure collapses to `undefined` so analytics never blocks a sign-in) behind a **3-second `MEMBER_LOOKUP_TIMEOUT_MS`** (`ghost-reader.ts`), and a miss is **sticky for the whole sign-in** because `/api/me` re-reads `session.identity` and never re-fetches — so one slow lookup silently un-identifies up to four hours of events. ⚠ The ticket's **description is stale** (it leads with a Google OIDC hypothesis **disconfirmed 2026-07-29**); its comment thread is authoritative, and it carries a 4-way fork for Forrest whose recommended first step is a log-based metric on the two existing WARNINGs. ⚠ Consequence to keep in view: `Signed In` is the instrument for measuring active members against the ~700 living brothers, and ~1/3 of brothers legitimately have no email and no Ghost account, so a uuid-less sign-in path would undercount **silently**. → **N153 (current — read before diagnosing any "missing property" in the Users grid)**: the two halves are **independent writers** on one person record. Book writes `Constitution ID`/`Role` and **never** an email; `$email` is written **only** by `default.hbs` (D139), so it is present ⟺ that brother visited the newsletter *signed in*, and its absence says nothing about Book, the Ghost record, or tracking prevention. ⚠ `book.hbs` is a **standalone template, not `{{!< default}}`** — the bridge never identifies, so the common *link → bridge → magic-link → Book* journey yields a permanent no-email profile (the production behaviour too, not a staging artifact). ⚠ The bridge page still loads the newsletter's full-library config via `{{ghost_head}}` (autocapture + 100% replay) under an **anonymous** device id, minting orphan clusters that carry **no `app` property** — excluded from OFC-369's `app = book` report by construction; don't conflate. ⚠ Only Book registers D62's `app` discriminator; nothing registers `news`, and **both halves emit an event named `Page View`**.
- Analytics delivery: D138 (two projects — Mixpanel-Staging + Mixpanel-Prod, each spanning Ghost *and* Book at its tier; **Book** ships the `mixpanel-browser` **core** build, with **session replay and autocapture deliberately off** — replay would ship other brothers' PII around the D5/D82 projection) → **D140** (7a-2's three corrections: the token define defaults to **empty** — analytics off — rather than to production, because there is no prod deploy path yet and CI/Playwright builds the real bundle every gate run, so a prod default would write synthetic events into the live members' dataset; the core build is imported by its **documented** specifier `mixpanel-browser/src/loaders/loader-module-core`, measured 30.5 KB vs 31.2 KB for D138's `dist/mixpanel-core.cjs.js`; and Book talks to `https://api-js.mixpanel.com` **directly**, resolving the proxy question D139 parked) → **D162 (current)** — Book now routes through the `mp.pbe400.org` first-party proxy, D140's deferral having expired on its own terms once **D161** made Book same-site with `pbe400.org`. ⚠ `MIXPANEL_API_HOST` and `firebase.json`'s `connect-src` are duplicated deliberately and must move together (a unit test asserts the literal); ⚠ Book's telemetry now depends on gladstone, a single VM — revisit before production. ⚠ If iOS events are *still* missing, suspect **OFC-369** (unidentified sessions excluded by cohort definition), not the proxy + **N124** (as built: the `analytics.ts` / `analyticsClient.ts` seam — the logic module imports no Mixpanel code so it is testable under the node-environment unit tests, and is a **no-op until a client registers**; page views key on route `handle` metadata, never a parsed pathname; `reset()` on sign-out is **unconditional**, including for uuid-less sessions; `Search Performed` carries a bucketed count only; D74 ceiling 250 → **270 KB**) + **N125** (the code review that corrected N124 — read them together) — current. ⚠ **`BLOCKED_PROPERTIES` in `analyticsConfig.ts` is load-bearing for P6:** Mixpanel attaches **`$current_url`** to every event by itself, and on Book a URL names a brother (`/brother/5247`) or carries the search box (`?q=`) — the route-pattern design governs only app-authored properties, and the first draft shipped the leak under a comment claiming it was impossible. ⚠ Identity keys on **`realRole`**, never `role` — "View as" would otherwise rewrite an admin's person profile (same class as N104). ⚠ Events carry D62's `app: "book"` super property, re-registered after `reset()`; it **cannot be backfilled**. ⚠ `assert:no-session-replay` is in `verify:gate`: the rrweb recorder is absent from the bundle but its *plumbing* is not, so `autocapture: false` / `record_sessions_percent: 0` are explicit and enforced, not assumed. ⚠ `ignore_dnt` defaults **false** in the library — D88's "retained" holds only because `init()` says so. Chain continues at **D139** (the Ghost side, which is deliberately the *opposite* configuration — the newsletter theme runs the full library with replay and autocapture on, and that asymmetry must not be "harmonized"): **Ghost-staging shares `mp.pbe400.org` unmodified** (Mixpanel routes by payload token, not hostname — CORS verified from a staging origin; the proxy is a transparent pass-through, *not* a pinned mirror), and the theme's identify block is `typeof`-guarded because the library comes from per-instance Code Injection outside the theme. ~~Open for 7a-2: whether **Book** uses the proxy at all~~ — ~~answered by D140: no, not yet~~ — **answered by D162: yes.** D140 deferred it only until Book was same-site with `pbe400.org`; **D161** made that true by moving Book-staging to `book-staging.pbe400.org`, so the deferral expired on its own terms rather than being overturned. The `api_host` define and the CSP `connect-src` origin moved **together**, as every earlier entry insisted they must, and a unit test now reads `firebase.json` and asserts it rather than restating the literal.
- Analytics taxonomy: D62 (original pre-P6 event list) → **D145**/**N131** (7a-4, OFC-296 — taxonomy fleshed out to ~14 events; a typed `events.ts` registry is the single source of truth for names + property shapes, wrappers funnel through one typed `emit()`; D62 reconciled as-built) → **N132** (7a-4 follow-up, OFC-315 — 17 more events: View As start/end, 5 Admin Tools actions incl. the banner message as the one free-text property, 6 chrome/nav clicks, and 4 **id-less** brother-status actions; the browser-blocker test-hygiene landmine) → **N152** (OFC-368 — `Constitution ID` **alone** additionally registered as a **super property** inside `identifyMember`, so events are attributable in the Events feed without a per-row profile lookup; the `AnalyticsClient` seam gained `register`) — current. ⚠ **N152 is deliberately NOT in `APP_SUPER_PROPERTIES`** — that set is re-registered by `reset()`, so an identity-scoped id placed there would staple a departed brother's id onto the next person's events on a shared machine (the misattribution N124's unconditional reset exists to prevent). ⚠ It rides **only** identified sessions (same uuid guard as `identify()`), so it does not make every event attributable — see the identity line's OFC-369 landmine. `Role` stays a person property. ⚠ **Browser content/tracking blockers used to silently eat Mixpanel events on Book** — that was true while Book talked to `api-js.mixpanel.com` directly (D140). **Superseded by D162:** Book now routes through the same-site `mp.pbe400.org` proxy, which stock blocklists do not key on. Heuristic blockers may still interfere, and "no events" now also has an infrastructure cause — the proxy is a single VM. ⚠ **View As events fire just before a hard reload** — pagehide-flush delivery, verify live. ⚠ **P6 still leans on `BLOCKED_PROPERTIES` stripping `$current_url`** (N125): several new events fire on id-/value-bearing URLs (`/brother/5247/edit`, `?classYear=1984`), so the clean app payload is only half the guard. Events carry **names from closed sets only** — field-group labels, filter *dimension* names, toggle/column keys — never ids, values, queries, or `Email`/`$email` (the newsletter asymmetry, D139). `Profile Viewed`/`Brother Starred` (Forrest's call, over the ticket's objection) are **id-less** — counted, never attributed; `Own` booleans mark own-record actions. ⚠ **`FILTER_DIMENSIONS` must track `filterParsers`** or a new filter goes untracked; `Consent Toggle Changed` counts interactions not saved choices; `Signed In` fires anonymous-then-ID-merged.

## Backups, restore & DR

- **Integrity job & DR runbook (D102's unbuilt half): D102 → D151 (current) — deferred out of Phase 7 into its own Phase 7.8**, between UAT and cutover (2026-07-26, Forrest's call): nothing downstream consumes it, its downside is bounded (a slower recovery, not lost data), and its value rises at cutover when the pipeline first holds real member records. D151 also fixes the five shape decisions the building session executes to — **Cloud Build + Cloud Scheduler** (never GitHub Actions: this repo is PUBLIC and its workflow logs would publish the member directory), a **separate persistent-but-empty `pbe-book-verify` project** whose per-run Firestore database is deleted at teardown (never a second database inside staging, which would break the restore CLI's `--confirm <project-id>` guard as a discriminator), a **thin** data-plane-only rehearsal, the image manifest **verified not resurrected**, and cadence **weekly until public launch then monthly**. ⚠ Detection is **presence-based only** — past Monitoring's 23.5h absence ceiling (D148) a job that stops being scheduled is invisible. Ticket **OFC-333**; cutover tail (repoint at prod) in `CUTOVER-PLAN.md`.
- D63 → **D101** (offline hardened restore; forensic privileged-roster log) → **D102** (≈24h RPO; ephemeral verification environment — ⚠ *never staging itself*, which is standing and always was; see D151); JSON export **N58**; automated tier as built **D147**; restore as built **D150** + **N137**, **live-tested end to end on staging N138** (7b-3, current — a manufactured disaster restored and verified; four platform-boundary defects found that no offline test could reach, incl. the `--object` path dying on its first real run). Restore is an **operator CLI** (`npm run restore --workspace apps/api`; `apps/api/src/tools/restore.ts`, pure halves in `data/restore.ts` + `data/restore-executor.ts`) — validate-then-replace, guarded by `--confirm <project-id>` and a maintenance pre-flight, with a safety snapshot before the first delete. Procedure: `infra/README.md`. Two D101 clauses are amended by D150 — the **forensic entry is written by the tool to Cloud Logging** (Book is down during a restore, so there is no `cloud_run_revision` for the audit sink to match; the sink filter carries a second clause for the tool's log name — ⚠ **the log name in `provision-observability.sh` and `RESTORE_LOG_NAME` in `tools/restore.ts` must agree, or the entry is written and silently never retained**), and the post-restore Ghost reconciliation is **report-only, no bulk re-push** (OFC-332). ⚠ **A restore is invisible until the instance is replaced** — the cache hydrates only on cold start. ⚠ **One-time per environment: re-run `provision-observability.sh`** or the forensic entry is written and never retained (the script is Forrest-run, not deploy-run — the PR #16 / #146 drift class). Only one validation rule is waivable — a *cross-profile* duplicate email, via `--allow-duplicate-emails`, because D97 anticipates one surviving in live data. Image objects are **not** restored (the manifest is verified by the integrity job — now **Phase 7.8**, D151).
- Automated backup (7b-2): **D147** — Cloud Scheduler → `POST /api/internal/backup`, sharing the roster's subject-pinned OIDC verifier (D58/D78); manifest derived from the profiles snapshot, never from a bucket listing; `objectCreator`+`objectViewer` but never `objectAdmin`; snapshot envelope **v2** — the D52 download stayed v1 until **D150** unified them (both producers now emit v2; the restore still reads v1 archives). Detection: **D147** (in-app pre-flight staleness check, alerts and proceeds) → **D148** (the external "never ran" backstop blocked — Monitoring's metric-absence window caps at 23.5h, shorter than a 24h cadence) → **D149** (current — cadence moved to **twice daily**, which makes the absence policy buildable; both thresholds 20h; the cadence is an *exercise-the-pipeline* and alerting decision, NOT an RPO one; hash-and-skip-if-unchanged explicitly rejected). ⚠ **The absence window is arithmetically coupled to the cadence** — returning to daily deletes that alert rather than coarsening it. Availability is a *separate* instrument (uptime check, OFC-329), deliberately not this one.
- Bulk ops: **D100** (no online bulk writes; restore is an offline maintenance event) → **D114**; maintenance screen D118 → **N69** (⚠ **the edge swap does not cover the site root** — Hosting prefers a matching static file over the rewrite, so `/` still serves the SPA during maintenance; measured in the 7b-3 live test, **OFC-334**, and why the restore's pre-flight probes `/api/health`); bounce reporting D120 → **N69**.
- Migrations: **D71** (code defaults vs versioned scripts); initial data load **D57** (external one-time tooling).

## Ops

- **D99** (trace IDs on the save path; consolidated periodic sysadmin job — audit read-only per N69); backend env vars **N3**.

## Testing & environments

- **D65** (Vitest + emulator; Playwright; committed fake-data generator) + **N23** (planted name collision) + **N4** (fixed dev ids).
- Staging/UAT: D72 → **D153** (the Ghost target amended — a **standing self-hosted ghost-staging** at `staging.pbe400.org` serves **both** the auth bridge and the Book→Ghost write path; D72's "additive route on the live Ghost Pro" and "stubbed write path" clauses are retired, the additive-route *mechanism* survives on the new host, and the stub remains only as the no-credentials fallback). ⚠ **The invariant is unchanged and absolute: nothing in dev or test points at production Ghost, either direction, until cutover.** ⚠ **Verify environment claims against `infra/environments/staging.env` and the code, never against this log** — the amendment happened in the world and went unrecorded for weeks, during which CODING-PROJECT-PLAN §4 told every fresh session that staging authenticates against the *live* newsletter (OFC-335). → **D108** + **N18** (staging wipe-reseeds every deploy) + N64 (UAT on test data, non-production) → **D132** (UAT = its own Phase 7.7 on staging in a frozen-autoseed window — code deploys continue, data survives; real-named testers via a private-GCS roster + `seed:staging-testers`, blank-beyond-identity profiles; UAT photo corpus; real magic-link flow required — email fix OFC-252; theme integration split off as Phase 7.6; plan `UAT-PLAN.md`, cutover items parked in the `CUTOVER-PLAN.md` stub) → **D160** (current for UAT's *exit criteria* — the "~8 testers completed the task script" threshold is **retired**: the cohort closed at **six** and the sent instructions deliberately dropped Appendix B's script, so the criterion was unsatisfiable on both premises. Participation is not a gate; the phase turns on bugs fixed, items triaged and wind-down executed. ⚠ **A tester who cannot sign in is a defect to chase, not a participation statistic** — and since OFC-351's pre-kickoff delivery smoke test was cancelled in favour of folding the check into the testers' first task, the kickoff email's "tell me if no sign-in link arrives" line is now the *entire* delivery check) → **D156** (current for tester provisioning — `seed:staging-testers` replaces `link:staging-tester`; testers occupy their OWN id block at **`TESTER_ID_FLOOR` #9001+**, never overwriting generated fixtures, so the collision pair and both admin fixtures survive. ⚠ **Above `FAKE_ID_FLOOR`, never below** — D65 reserves <5001 for real signing numbers and tester rows carry real names/addresses; enforced in the parser. Blank `profileId` auto-assigns; **first row defaults to admin**, rest to brother. ⚠ **Ghost: match by EMAIL, delete by LABEL** — Ghost enforces email uniqueness globally, so a label-only scope 422s against a pre-existing account (found in live testing); `selectReconcileScope` keeps the two apart. ⚠ `send_email=false` is load-bearing since D154. Removing a tester = drop the CSV row + reseed) → **D155** (current for UAT *fixtures* — the photo corpus is encoded **once** by `prepare:uat-photos` through the production `encodeHeadshot` and parked as ~12 MB of WEBP, rather than re-encoding 544 MB on every deploy; `HEADSHOT_SIZE`/`THUMBNAIL_SIZE` moved to `@pbe/shared`'s `images.ts` and a `manifest.json` records them so a parked set drifting from the encoder is **detectable**. Fixtures live in a **separate** private bucket `pbe-book-staging-uat` — ⚠ never a prefix on the image bucket, whose `objectAdmin` would put the tester roster's real PII inside the running app's blast radius; ⚠ the deployer's bucket-scoped grant there is *redundant* documentation of intent, not a read-only restriction. 408 photos dress the lowest 408 of 438 `hasHeadshot` profiles, deterministically by id, **never repeating a face**; every failure path falls back to placeholders with a warning rather than failing the deploy) → **D154** (current for ghost-staging mail — the OFC-252 prerequisite closed: outbound mail moves off the Mailgun **sandbox** onto **`staging.pbe400.org` as a verified sending domain**, free tier, SPF+DKIM only, click/open tracking off. ⚠ **Read before touching Ghost-staging's mail config:** the sandbox's *provider-enforced* "can never reach a real brother" guarantee is gone, and now rests entirely on two configuration invariants — the staging member list stays fake, and `bulkEmail` stays unset. Apex-SPF fallout spun out as OFC-350); backup-verification runs in a **separate** ephemeral project, not staging — **D151** (Phase 7.8, after UAT); N82 (docs-only merges skip the post-merge CI re-run → no deploy; PR gate unfiltered) → **D143** (the post-merge re-run is gone outright: `ci.yml` triggers on `pull_request` only, and `deploy-staging.yml` triggers on the push to `main` **directly** rather than on a CI `workflow_run` — N82's `paths-ignore` moved onto that trigger, so the docs-only-no-deploy outcome is unchanged. The PR run — required `strict`, i.e. branches-up-to-date, per **N91** — is the single authoritative gate, and that `strict` requirement is what replaces the post-merge run's guarantee; a genuinely concurrent merge race would be answered by a merge queue, not by re-running the gate. ⚠ **The production path is deliberately untouched** — prod remains a manually-dispatched promotion that runs the gate against the commit being promoted, *never* merge-triggered: CODING-PROJECT-PLAN §5, `CUTOVER-PLAN.md`, OFC-253) → **N139** (current for the ignore list — it gains the non-shipped Markdown under `infra/`, `tools/` and `ghost-bridge/` that N82's three paths missed, each as an `X/*.md` + `X/**/*.md` pair. ⚠ **Never blanket `infra/**`** — `infra/environments/staging.env` must deploy — and ⚠ **never blanket `**/*.md`** — `packages/help-content` (D111) and `apps/web/src/content` both ship Markdown. Re-derive the list **from the tree**, never extend it from memory; unverified until a Markdown-only merge is observed to produce no deploy); **N90** (reseed wipes `users`/stars too, deterministic deploy; column prefs are a localStorage residual reset in-app, OFC-197).
- CI/deploy action pins: **N87** (all `uses:` bumped to latest Node-24 majors, OFC-62; the Firebase-CLI step's `@v7` action pin is orthogonal to its load-bearing `node-version: "20"` install — don't collapse them).
- CI gate integrity: **D141** (`assert:gate-in-sync`, first gate step of both pipelines: `verify:gate` and `ci.yml` must list identical npm-run step sequences, order included — the two lists stay hand-maintained on purpose (per-step timing/attribution, `ci-timing.mjs`) and the guard sits in *both* lists so deleting it from either side fails the other; grew from N125 item 3 / OFC-297; **activates tiered Gate 4 merge-on-green for this repo**, re-raising N91's admin-bypass question — `enforce_admins` is Forrest's call, tracked on OFC-297).

## Accessibility & help

- A11y: D32 → D67 (three verification layers) → **D79** (WCAG 2.2 AA, CI-gated) → **D111** (help split along the WCAG line); modals: **N145** (focus return is `ModalDialog`'s job, not the platform's — callers close by *unmounting*, so no `close()` fires, and the page is inert until it does, which silently ate the bug-report dialog's own attempt for two months; OFC-353). Automated axe covers text contrast (1.4.3) but **not 1.4.11 non-text contrast** of custom indicators — **OFC-261** (Phase 7/7c) adds a token-pairing contrast matrix + a manual state-indicator check (N101, the consent-off-ring miss). ⚠ **D165 grew that matrix**: the course-chip palette went from 6 hand-picked families to 25 + a reserved neutral, so OFC-261 must be run against the generated palette, not the six-colour one it was scoped around — the generator machine-checks every pairing at build time (worst 7.14:1 light / 9.30:1 dark) and `assert:chip-tokens` gates it, which makes the audit cheaper, not larger.
- Help model: **D53** (layered, embedded, toggle-tips) → **D111** → **N102** (the `CircleHelp` toggle-tips wired across every page via `HelpToggleTip`/`ControlHelp` — Radix Popover, D111's above-baseline layer; registry is the single source read by the UI and the 6c manual; interim `<details>` `HelpTip` retired) → **N103** (live-test polish: switch `?` drops the counterfactual, keeps only static context) → **N116** (6c-1: the **About page** — the first standalone guidance *page*, `/about` from the avatar menu; copy in `src/content/about.md` compiled to HTML at **build time** by `aboutHtmlPlugin` so no Markdown parser ships; the repo's first `dangerouslySetInnerHTML`, made safe by build-failing guards in `compileAboutHtml`; About's copy is its **own** source, not registry-generated — D53's no-drift rule is about per-control help, not descriptive prose) → **N118** (6c-2: D53's promise finally *enforced* — USER-MANUAL §10 is generated from the registry in place between markers by `npm run docs:help`, and `assert:help-manual` in `verify:gate` fails the build on drift; grouping is by key prefix and an ungrouped key throws rather than being silently dropped) → **N119** (6c-2: the manual's illustrations are **generated** — `npm run docs:screenshots` drives the production bundle against fake-data mocks into `docs/images/`, byte-identical run to run so image staleness shows up as a real diff; dark mode throughout, outside the CI gate by design; **re-run it after any copy or chrome change**) → **N120** (6c-2 close: the manual finished against the built app — the footer's privacy link made real (`withAboutLink` → `/about#privacy`, needing slugged heading ids in `compileAboutHtml` *and* a hash-scroll effect, since a client-side nav doesn't scroll; OFC-281), the two numeric Directory filters given toggle-tips (OFC-283), the narrative's stale claims corrected and the N116 naming rule applied throughout, "Status: draft" removed; **amends N119** — the phone shot is bistable ~1 run in 10, OFC-284 — current); → **N146** (Stage 1.4: the About page finally *names* the manual and links it, so testers can find the deepest help layer at all; ⚠ the URL is pinned by an e2e assertion **on purpose**, because OFC-314 will move `USER-MANUAL.md` — that move must update the expectation, never loosen it); consent-copy pattern **D113** (counterfactual-in-`?` since removed, N103).
- Product naming (member-facing copy): **N116** — "PBE Address Book" on first use, "the Address Book" thereafter; bare "Book" is internal shorthand only (code, comments, docs). ⚠ The rule **stops at member-facing copy**: the admin Ghost alignment-audit surface deliberately keeps "Book" as a system name contrasted with "Ghost" ("Book value | Ghost value") — don't "fix" it. No mechanical guard beyond a unit assertion over `about.md`.
- Icons: **N101** — `lucide-react` is the app icon set (one-sweep 6a adoption); every icon a monochrome Lucide glyph inheriting `currentColor` + `aria-hidden` (no emoji/multicolour icons); `DebrotheredMark` and the Avatar silhouette kept bespoke.

## UI shell & app-wide

- **D30** (client prefs in localStorage) + **N15** → **N104** (the column-lens "foreign shared link" test compares `cols` to the saved value, not mere URL presence — so an impersonation hard reload no longer misreads the user's own view as foreign and breaks "Reset to default columns"; OFC-263, refines D30/D31; residual OFC-274); **D31** (three-bucket URL/state model) → **N79** (adds a fourth bucket: per-instance transient row selection) + **N99** (stars hoisted into that same per-instance shell context, shared by the Directory and the Profile — OFC-256) + **N80** (masthead clean-slate `reset` vs. place-preserving back-nav); **N21** (full-bleed shell); **N24** → **D131** (masthead font-size **and** theme toggles now live in the avatar menu; PBE News top-bar link restored; wordmark truncates so the bar never clips — current) + **N93** (icon-only masthead controls use `sr-only`, not `hidden`, so they keep an a11y name on a phone — WCAG 4.1.2) + **N94** (the PBE News link URL is environment-specific — build-time `BOOK_PBE_NEWS_URL`, defaults prod, staging.env overrides to ghost-staging); **D117** → **N57** (admin-set system banner); **D119** (loading overlay); **N77** (client-rendered 404 for unknown URLs); **N78** (long-lived-tab new-version toast).

## Admin surfaces

- **D52** (control-panel surfaces; amended by D55/D63/D68/D100/D114); **D51** + ~~N50~~ → **D128** (segmented role control now reads `record.role`; role write re-pathed to `PUT /api/profiles/{id}/role`; last-admin over the ProfileCache); N49 (4c-2 privileged-action slice).
- Bug reports: **D121** → **N60** (Book receives bugs, doesn't track them — so "Copy report" is the entire Book→tracker path) → **N61** (endpoints; two-value status; DELETE verb) → **N151** (current — the clipboard block is shaped for a Linear paste: description first, diagnostics in a `>>>` collapsible **code fence**, because Linear's paste honours neither CommonMark hard-break spelling; measured, not assumed).

## Process & scope

- **D1** (doc homes), **D11** (MVP line; → D27, D60); re-plans **N30** (Phase 4), **N39** (4c split), **N59** (Phase 5.5 batches), **N67** (5b split), **N62** (Phase 7.5), **D132** (UAT → Phase 7.7 + theme-integration Phase 7.6; `UAT-PLAN.md` + `CUTOVER-PLAN.md` stub), **N95** (second Todo triage: sessions 5.5i–5.5l appended, order 5.5k→l→i→j; the rest of the backlog scheduled onto Phases 6–8 by Linear label; pre-session ruling D133), **N122** (Phase 7a splits into 7a-1 identity / 7a-2 Mixpanel skeleton / 7a-3 logging & observability; 7b and 7c untouched so their labels stay valid) → **D152** (current — the tracker moves to **`Stage N.M` labels**; the calendar splits out into **`LAUNCH-SCHEDULE.md`**, which is the single source for the schedule *and* the phase→stage mapping, while `CODING-PROJECT-PLAN.md` stays the single source for scope and gates). ⚠ **Phases and stages are orthogonal and not one-to-one** — one stage may hold fragments of two phases plus ops work belonging to none; a reader forcing a bijection will wrongly read the plan as broken. ⚠ **Nothing was renumbered:** the plan carries an additive overlay only (a §1 paragraph, italic forward-pointer trailers on the five incomplete phases, a "Scheduled as" column on §8, a §7 closer). A renumber would break the hundreds of by-session-number back-references threaded through this log — the same reasoning that made D151 leave session number 7b-4 permanently vacant. **7b-3 is the last work completed under phase numbering.**
- Licensing: **D127** (repo is MIT — code only; PBE names, marks + brand-artwork files reserved to Phi Beta Epsilon Corporation).
- Public-repo hygiene: **N158** (a brother's name never appears in the tree, a commit message, or a PR description — write "the reporter" / "a UAT tester"; names in **Linear** are correct and wanted, the boundary being the public/private one). ⚠ Unenforceable by any gate — a check would have to list real names to find them, and two of the three surfaces are outside the tree; it rests on repo `CLAUDE.md` + the dev-workflow skill. ⚠ Three pre-N158 commit messages retain two names and are **deliberately not rewritten** — don't force-push `main` to "finish" it. Also records the OFC-372 audience-profile correction and which surfaces are frozen against both sweeps.

---

*When the initial build closes, this index becomes the skeleton of the as-built digest: the chronological log freezes into `history/` and the digest replaces it as the read-first artifact (see the dev-workflow skill, "The decision log").*
